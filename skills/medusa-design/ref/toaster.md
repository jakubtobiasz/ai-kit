# Toaster

This primitive is in `@medusajs/ui` 4.2.1. It may not have a dedicated docs.medusajs.com page.

Host for toast() notifications. Mount once.

## When to open this file

Open this file when the view needs `Toaster`.

## Kit props

This component is based on the [Toaster component of the Sonner library](https://sonner.emilkowal.ski/toaster).

## Atlas

Kit page mounts `<Toaster />` once next to TooltipProvider.

For a simple screen, follow the locked preview in `views.md`. Do not invent a new layout unless the human asks.
