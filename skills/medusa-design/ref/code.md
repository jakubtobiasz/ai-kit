# Code

This primitive is in `@medusajs/ui` 4.2.1. It may not have a dedicated docs.medusajs.com page.

Inline code chip.

## When to open this file

Open this file when the view needs `Code`.

## Kit props

This component is based on the `code` element and supports all of its props

## Atlas

Kit Data display. Inline identifiers such as `prj_storefront`.

For a simple screen, follow the locked preview in `views.md`. Do not invent a new layout unless the human asks.
