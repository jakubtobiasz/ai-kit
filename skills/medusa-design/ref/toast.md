# Toast

Docs: https://docs.medusajs.com/ui/components/toast
Import from `@medusajs/ui`. Installed kit is `4.2.1`.

A toast helper. Mount Toaster once.

## When to open this file

Open this file when the view needs `Toast`, or a related control from the index row for `toast`.

## Usage

## Kit props (`@medusajs/ui` 4.2.1)

This component is based on the [Sonner](https://sonner.emilkowal.ski/toast) toast library.

## Atlas

Kit Feedback. Call `toast()` after `Toaster` is mounted.

For a simple screen, follow the locked preview in `views.md`. Do not invent a new layout unless the human asks.
