# Docs index

Start here before you write or read a living note.

| Topic | Path | One-liner |
| --- | --- | --- |

No rows yet. After the first note, append a row. Shape:

`| catalog-import | [catalog-import.md](./catalog-import.md) | How CSV import maps columns. |`

A grown topic is a directory. The path is `catalog-import/` and the row links `[catalog-import/](./catalog-import/_index.md)`.

## How to use (agents)

1. Read this table. Do not glob the whole `docs/` tree.
2. Open a topic file or directory only when the row matches the question.
3. For a new note: follow the `ai-dir` skill. Copy [`TEMPLATE.md`](./TEMPLATE.md). Append a row here in the same change.
4. Binding next steps belong in [`.ai/adr/`](../adr/_index.md).
